#include <bits/stdc++.h> 
vector<int> wiggleSort(int n, vector<int> arr) {
    for(int i =1;i<n;i++){
        if(((i%2 == 1) && arr[i]<arr[i-1]) || ((i%2 == 0) && arr[i]>arr[i-1])){
            swap(arr[i], arr[i-1]);
        }
    }
    return arr;
}
